/**
 * @acp:domain library
 * @acp:layer utility
 */
import base from "./base.js";

/**
 * ESLint configuration for library packages
 * Extends base config with JSDoc rules
 */
export default [...base];
